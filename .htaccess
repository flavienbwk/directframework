# Direct Framework - Under MIT license
# The following lines are critical for the good working of the framework.
# Please do not remove any!
Options +FollowSymlinks
RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

RewriteRule assets((/(.*)[.]([a-z]){1,4}){1,}) Components/assets/$1 [L]
# Allows to create clear links.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ((.*)/){1,}(.*) Components/Router.php?path_dir=$1&path_file=$3 [L]

# Used for index folders
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule (([A-Z]|[a-z]|[0-9])+){1,} Components/Router.php?path=$1 [L]
# Replace the index.php file.
RewriteRule ^$ Components/Router.php?index [L]